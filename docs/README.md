# Ollivo — рабочая документация

Источник правды по проекту: решения, ход работ по фазам, замеры и находки.
Всё, что решено или проверено, записывается здесь, а не в переписке и не в личных заметках.
Обзор для пользователей — [README.md](../README.md), передача проекта целиком — [HANDOFF.md](../HANDOFF.md),
правила работы — [CLAUDE.md](../CLAUDE.md), выпуск версий — [RELEASE.md](../RELEASE.md).

Сайт — ollivo.ru. Старое рабочее имя — `gui_Ai`; от него осталась только тестовая папка `D:\gui_Ai_lab` (вне репо).

## Суть
Десктоп-программа для запуска локальных открытых моделей (HuggingFace и т.п.) обычными людьми.
Установил → выбрал файл/модель → чат. Текст, картинки, видео, аудио в одном окне, голосовой ввод,
вложения. Два режима: «Простой» (всё настраивает сама) и «Профи» (все параметры, логи, терминал).
Конкуренты: ComfyUI (сложно), LM Studio / Jan / Ollama (только текст), Pinokio (установщик, не единый UI),
Stability Matrix, Fooocus, Msty.

## Архитектура
Идёт фаза 2 (подробно — [Фаза 2](phase-2.md)); фаза 1 закрыта. Репозиторий: `src/` — React, `src-tauri/` — Rust-ядро,
`spikes/` — прототипы фазы 0 ([Фаза 0](phase-0.md)). Общая схема (подробно — в [План](plan.md)):
- Оболочка: Tauri 2 + React + TypeScript.
- Ядро: Rust — загрузки, манифест версий, проверка железа (NVML), запуск движков. Данные — JSON-файлы (настройки, библиотека, разговоры).
- Движки: llama.cpp (текст/зрение), whisper.cpp (голос), ComfyUI headless (картинки/видео), TTS.
- Python (uv + встроенный 3.12) только для ComfyUI, ставится по требованию.
- Тип модели определяется по файлу (GGUF / safetensors) → выбор движка и «светофор».
- Обновления программы, манифест движков и зеркало — в S3 Timeweb, как в «buffer_pc» (личная заметка, вне репозитория).

## Документы
- [План](plan.md) — единый план от нуля до 1.0: стек, фазы, сроки, обновления через S3.
- [Фаза 0](phase-0.md) — результаты разведки рисков: model-probe, находки про Pascal/CUDA 13.
- [Фаза 1](phase-1.md) — каркас Tauri, Rust-ядро: железо, менеджер загрузок; ход работ и замеры.
- [Фаза 2](phase-2.md) — чат с текстовыми моделями: библиотека моделей, каталог, сам чат.
- [Настройки](settings.md) — что должно быть в настройках: простой режим и «Профи», что обязательно к MVP.
- [Установка](install.md) — установщик, докачка зависимостей, мастер первого запуска, UX экрана установки.
- [HANDOFF.md](../HANDOFF.md) — сжатая передача проекта: решения с обоснованием, что сделано и как,
  что осталось и как, грабли, правила. Выжимка этой папки для того, кто видит проект впервые.

## Решения
- Название: **Ollivo** (только английское), сайт ollivo.ru. Отброшены Stash, Domovoi, Lumaro и др. — конфликты с существующими продуктами (см. [Фаза 0](phase-0.md)).
- Идея: задачи вместо моделей («удалить фон», «озвучить текст») — для простого режима.
- ComfyUI использовать как скрытый движок, а не конкурировать с ним.
- MVP: только Windows + NVIDIA.
- Установщик маленький, всё докачивается само; системные Python/Git/CUDA не нужны (см. [Установка](install.md)).
- Компоненты ставятся по задаче: чат (llama.cpp) без Python, torch+ComfyUI — только для картинок/видео.
- Главный бэкенд — Rust-ядро Tauri, а не Python: логика установки должна работать до появления Python.
- Автообновление через Tauri updater + S3 Timeweb, как в ClipVault. Делается в фазе 1, а не в конце.
- Сборки движков выбираются по compute capability: CUDA 13 не поддерживает GTX 10xx и старше → для них CUDA 12.
- Кандидат: чат по умолчанию на Vulkan-сборке llama.cpp (30 МБ против ~600 МБ у CUDA), CUDA — опциональный ускоритель по замеру. Проверить на RTX.
- torch: GTX 10xx → cu126, RTX → cu130 (требование ComfyUI). Большие колёса качает наш менеджер загрузок, uv ставит из локальной папки; uv — с `--no-bin`.
- Менеджер загрузок качает в несколько потоков (с HF в 7,6 раза быстрее) и сверяет SHA256.
- Движки живут в Job Object с kill-on-close: после закрытия Ollivo не остаются висеть в памяти и видеопамяти.
- Мастер первого запуска сам ставит VC++ Runtime (нужен llama.cpp) через официальный установщик Microsoft.
- Прокси (HTTP/SOCKS5) — во всех сетевых запросах ядра; пароли и токены — только в диспетчере учётных данных Windows.
- История разговоров — JSON-файл на разговор, поиск — перебором файлов, без SQLite: 23 МБ разговоров ищутся за 0,2 с ([Фаза 2](phase-2.md)).
- Пресеты вместо параметров: окно знает только названия ролей и манер, числа и промпты — в ядре (`presets.rs`). Направление перевода определяет ядро по буквам вопроса: маленькие модели условия в промпте не выполняют.
- Подписи кода (SmartScreen) пока нет — нет денег, бесплатно Microsoft не выдаёт. Обходимся инструкцией на сайте и VirusTotal.

## TODO
- [x] Выбрать MVP: Windows + NVIDIA.
- [x] Написать план (см. [План](plan.md)).
- [ ] Фаза 0: разведка рисков (ход работ — [Фаза 0](phase-0.md)).
  - [x] model-probe: тип модели по файлу + светофор.
  - [x] llama-server: работает; CUDA 13 на Pascal падает; Vulkan быстрее CUDA 12 на GTX 1080.
  - [x] uv + Python + torch cu126, ComfyUI headless: картинка SD 1.5 за 9 с, прогноз памяти совпал.
  - [x] Калибровка SDXL: 60 с на 1024², 6,5 ГБ = прогноз.
  - [x] Название: Ollivo, сайт ollivo.ru.
  - [ ] Пользователь Windows с кириллицей и пробелом (создаёт пользователь) — проверка путей.
- [x] Товарный знак Ollivo в WIPO: только Индия, классы 6 (фурнитура) и 20 (мебель) — с софтом (9, 42) не пересекается.
- [ ] Проверить Роспатент (ФИПС): Ollivo, Olivo, Оливо.
- [ ] Позже (к 0.1–0.5): зарегистрировать свой знак Ollivo в РФ, классы 9 и 42.
- [x] Фаза 1: каркас и установка (ход работ — [Фаза 1](phase-1.md)), кроме проверки на чистой Windows.
- [ ] Фаза 2: чат с текстовыми моделями (ход работ — [Фаза 2](phase-2.md)).

## Лог
- 2026-09-21 — заметка создана, записана идея и черновик архитектуры.
- 2026-09-21 — решено: Windows+NVIDIA, докачка зависимостей при установке; заметка [Установка](install.md).
- 2026-09-21 — продуман UX экрана установки (раздел в [Установка](install.md)).
- 2026-09-21 — составлен единый план до 1.0 ([План](plan.md)); главный бэкенд — Rust-ядро; обновления через S3, без подписи кода.
- 2026-09-21 — начата фаза 0: git-репозиторий, прототип `spikes/model-probe` (GGUF/safetensors/Whisper → тип, движок, светофор); заметка [Фаза 0](phase-0.md).
- 2026-09-21 — llama-server проверен на GTX 1080: CUDA 13 падает, Vulkan 81 ток/с против 52 у CUDA 12 (3B Q4); многопоточная загрузка с HF в 7,6 раза быстрее.
- 2026-09-22 — uv + Python 3.12 + torch cu126 + ComfyUI v0.37 headless на GTX 1080: SD 1.5 512² за 9 с, пик памяти +3,1 ГБ = прогноз; веса fp16 на Pascal. venv 5,2 ГБ.
- 2026-09-22 — SDXL на GTX 1080: 60 с/картинка 1024², светофор откалиброван; подбор названия: кандидаты Ollivo, SnugAI.
- 2026-09-22 — выбрано название **Ollivo**, сайт ollivo.ru; папка установки `C:\Ollivo`, префикс в S3 `ollivo/`.
- 2026-09-22 — WIPO: знаки Ollivo есть только в Индии, классы 6 и 20 — конфликта с софтом нет.
- 2026-09-22 — папки переименованы: репозиторий `D:\GitHub\Ollivo`, заметки `Projects/Ollivo`.
- 2026-09-22 — начата фаза 1: каркас Tauri 2 + React, Rust-ядро — `hardware` (NVML, выбор сборки CUDA 12/13/Vulkan, диски) и `download` (Range в 8 потоков, докачка, зеркала, SHA256); заметка [Фаза 1](phase-1.md).
- 2026-09-22 — манифест движков (llama.cpp b11081), установка движка по манифесту (проверено: Vulkan за 18,7 с), прокси HTTP/SOCKS5 с проверкой; заметка [Настройки](settings.md).
- 2026-09-22 — мастер первого запуска (проверки ПК, VC++, диск, сеть, движок), зеркало и токен HuggingFace.
- 2026-09-22 — менеджер процессов движков (Job Object, логи, готовность) и запуск llama-server; Qwen2.5 0.5B: 158 ток/с. Тестовая папка фазы 0 оказалась в `D:\Ollivo` (переименована вместе с остальным).
- 2026-09-22 — отмена долгой загрузки модели: «Отменить» срабатывает сразу, статус не зависает.
- 2026-09-22 — кнопка «Починить» для движков (сверка SHA256 файлов, переустановка битого); workflow GitHub Actions для сборки установщика.
- 2026-09-22 — экран установки для новичка (шаги, время словами, пауза, автоповтор при обрыве сети, прогресс на панели задач).
- 2026-09-22 — автообновление: плагин updater, каналы stable/beta, `publish-update` (ключ + пароль в диспетчере учётных данных), `RELEASE.md`; 0.0.2 выложена в beta.
- 2026-09-23 — обновления заработали: причина была в BOM у описания версии; 0.0.1 → 0.0.2 прошло вживую (нашло, скачало, поставило, перезапустилось). Фаза 1 закрыта, кроме проверки на чистой Windows.
- 2026-09-23 — начата фаза 2: библиотека моделей — файл добавляется кнопкой или перетаскиванием, тип и «светофор» определяются по заголовку (разбор перенесён из прототипа фазы 0); экран «Модели».
- 2026-09-23 — подхват уже скачанных моделей: папки LM Studio, Ollama (имена из манифестов), ComfyUI и своя папка; файлы не копируются.
- 2026-09-23 — чат со стримингом: ответ появляется по ходу, «Остановить» обрывает за 0,3 с; вкладка «Чат» стала первой. История разговоров пока не сохраняется.
- 2026-09-23 — каркас окна: меню слева, чат на всю высоту с прибитым вводом; история разговоров (файл на разговор, список в меню, продолжение и удаление).
- 2026-09-23 — автонастройка запуска: слои на видеокарту и память разговора считает ядро перед самым стартом, окно шлёт только путь; в карточке видно, кто считает.
- 2026-09-23 — в ответах появилась разметка (markdown-it): списки, таблицы, код с «Копировать»; присланный HTML не исполняется, ссылки уходят в браузер. Плюс «Ответить заново».
- 2026-09-23 — каталог моделей: встроенная подборка из 8 моделей (Qwen3.5, Gemma 4, LFM2.5) с точными
  размерами и хешами плюс живой поиск по HuggingFace. У каждого варианта сжатия видно вес, смысл
  («Q4 — обычный выбор») и прогноз скорости на этом ПК; скачанное сразу попадает в библиотеку.
- 2026-09-24 — собрано `HANDOFF.md`: описание проекта для передачи в другой ИИ.
- 2026-09-24 — документация переехала в репозиторий (`docs/`) и стала источником правды;
  в Obsidian осталась карточка проекта со ссылками сюда.
- 2026-09-26 — поиск по разговорам: поле над списком, все слова запроса без учёта регистра и «ё», кусок реплики с подсветкой. Перебор файлов, 23 МБ — 0,2 с, SQLite не нужен. В окне не проверен.
- 2026-09-26 — роль (помощник, переводчик, программист) и манера «Точнее · Обычно · Креативнее» вместо параметров; числа и промпты только в ядре. Направление перевода решает ядро — 3B-модель с условием в промпте не справилась.
- 2026-09-26 — лицензия модели в карточке: при скачивании из каталога запоминается со страницы HF и показывается одной фразой — можно ли для работы; для особых условий — ссылка на страницу модели.

## Как вести эту папку
- Решили что-то — строка в «Решения» здесь, с причиной. Причина важнее самого решения.
- Сделали что-то заметное — раздел в заметке своей фазы (как именно сделано и почему так),
  строка в «Лог» вида `- ГГГГ-ММ-ДД — что сделано` и галочка в «Осталось».
- Замеры и грабли записываются с числами и железом, на котором получены.
- Устаревшее исправляем или удаляем, а не копим рядом.
- Значения паролей, токенов и ключей сюда не попадают — только где они лежат и как называются.
